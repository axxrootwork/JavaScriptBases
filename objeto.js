const obj = {
    nombre: "Ale",
    edad: 30,
    trabajo: "Desarrollador",
    ciudad: "Mexico"
};

  // Desestructuración de tres claves
const { nombre, edad, ciudad } = obj;

  // Imprimir los valores desestructurados
console.log(nombre);
  // Ale
console.log(edad);
  // 30
console.log(ciudad);
  // Mexico
